import React from 'react';
import DynamicTitle from '../DynamicTitle/DynamicTitle';

const Blog = () => {
    DynamicTitle('Blogs')
    return (
        <div>
            <h2>Blog</h2>
        </div>
    );
};

export default Blog;